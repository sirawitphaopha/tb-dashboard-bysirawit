---
name: testing-proof
description: ใช้ก่อนบอกว่างานเสร็จทุกครั้ง — วิธีเทสด้วยการอ่านโค้ดที่จับบั๊กซึ่งการยิงดูผลจับไม่ได้ วิธีเขียนและอ่านผลตัวตรวจไม่ให้ตัวตรวจโกหก วิธีไล่บั๊กที่เกิดกับเครื่องคนอื่นโดยขอหลักฐานก่อนเดา และกฎเรื่องภาพ — แตะหน้าจอเมื่อไหร่ต้องเปิดภาพดูก่อนรายงาน อ้างว่าทำแล้วต้องแนบภาพครอปเฉพาะจุด
---

# เทสและพิสูจน์ — กฎกลาง ใช้ได้ทุกโปรเจกต์

> 🗺️ เปิดคู่กับ — **`machine-tools`** เรื่องกับดักของเครื่องนี้ · **`working-with-gun`** เรื่องกฎการรายงาน
> ตารางเต็มว่างานไหนต้องเปิดสกิลไหน อยู่ที่ `working-with-gun` ข้อ 0.0

> **แยกออกมาจาก `working-with-gun` เมื่อ 5 กันยายน 2569** ตามที่พี่กันสั่ง
**เลขข้อคงไว้ตามไฟล์แม่เดิม** (`working-with-gun`) เพื่อให้การอ้างอิงเก่าทุกจุดยังใช้ได้

> เพราะไฟล์แม่ยาว 1,680 บรรทัด **ท้ายไฟล์เสี่ยงถูกตัดตอนบทสนทนายาว**

🚨🔴 **แยกไฟล์ไม่ได้แปลว่าอ่านน้อยลง** — จะบอกว่าอะไรเสร็จ ต้องเปิดไฟล์นี้อ่านให้จบก่อน

## 3.6 🔍 ไล่บั๊กที่เกิดกับ "เครื่องคนอื่น" — ขอหลักฐานก่อนเดา

พี่กันเจอปัญหาบนเครื่องที่แคลร์เข้าไม่ถึง (เครื่องห้องยา · มือถือเพื่อน · เครื่องผู้ใช้จริง)

- 🔴🔴 **ขอ F12 → แท็บ Network + Console จากเครื่องที่พังจริง ตั้งแต่ข้อความแรก ห้ามเดาสาเหตุก่อน**
- **Console อย่างเดียวไม่พอ** — คำขอที่ได้ 404 หรือ 500 จะไม่ขึ้นอะไรในคอนโซลเลย
  ต้องดูแท็บ Network ถึงจะเห็นว่ายิงไปแล้วโดนตีกลับ
- 🚨 **ห้ามใช้ "เปิด URL ตรง ๆ ในเบราว์เซอร์แล้วได้ 200" เป็นหลักฐานว่าหลังบ้านไม่พัง**
  คนละกลไกกับการเรียกจากหน้าเว็บ (คุกกี้ · หัวคำขอ · CORS · วิธี POST ไม่เหมือนกัน)

🔴 **เคยพลาดจริง 12 ส.ค. 2569** เดาสาเหตุผิด **6 รอบติด** — เน็ตช้า · ตัวบล็อกโฆษณา ·
localStorage · สิทธิ์ Google · นโยบายองค์กร · DNS · เสียเวลาพี่กันทั้งบ่าย
พอได้ดูแท็บ Network จริง **จบใน 1 นาที**

---

## 4.5 🔍 เทสด้วยการ **อ่านโค้ด** เสมอ ไม่ใช่แค่ยิงดูผล (พี่กันสั่ง 4 ก.ย. 2569)

> *"เธอต้องเทสโดยการอ่าน code ทุกครั้ง ถือว่าเป็นกฎ ยิ่งงานใหญ่ ยิ่งต้องเทส ต้องตรวจ"*

### ทำไม

งานย้ายหลังบ้านของ ME-DRP ผ่านการทดสอบทุกอย่างแล้ว — ยิงทุกเส้นทาง 6 จุด · วงจรเขียน 14 จุด ·
กดปุ่มจริงในเบราว์เซอร์ · เทสบนเว็บจริง **ทั้งหมดเขียว**

พอพี่กันสั่งให้ "ตรวจทานอีกรอบ" แคลร์กลับไปยิงเทสซ้ำอีกรอบ แล้วรายงานว่าเสร็จ
พี่กันต้องทักว่า *"เราหมายถึงให้เธออ่าน code เพื่อเช็คนะ"*

**พออ่านโค้ดจริง เจอ 3 เรื่องที่การยิงเทสไม่มีทางจับได้เลย**

| เจออะไร | ทำไมการยิงเทสจับไม่ได้ |
|---|---|
| ป้ายในบันทึกข้อผิดพลาดเขียนคนละรูปแบบกับที่เหลือ (`api/auth POST` vs `incidents.POST`) | เว็บทำงานปกติทุกอย่าง จะรู้ตัวตอนเปิดบันทึกแล้วค้นหาไม่เข้าชุดกัน |
| `/api/health` เป็น route เดียวที่ไม่มีตัวจดบันทึกครอบ | ต้องให้ตัวมันเองพังก่อนถึงจะเห็น |
| `/api/bootstrap` สร้างไว้แต่ไม่มีใครเรียก (ยังยิง 3 เส้นแยกอยู่) | ทุกเส้นตอบ 200 หมด ข้อมูลครบหมด เทสเขียวหมด |

ข้อสุดท้ายพอแก้แล้ว **คำขอตอนเปิดเว็บลดจาก 4 เหลือ 2** — ของที่ทำไว้แล้วแต่ลืมต่อสาย

### วิธีอ่านโค้ดตรวจ — ตรวจ "ความสม่ำเสมอ" ที่การยิงเทสมองไม่เห็น

```bash
# ① ทุก handler มีด่านตรวจสิทธิ์ครบไหม (นับให้ตรงกัน)
for f in $(find app/api -name route.ts); do
  echo "$f · handler $(grep -c 'export async function \(GET\|POST\|PATCH\|PUT\|DELETE\)' $f)" \
       "· requireAuth $(grep -c 'await requireAuth' $f)" \
       "· catch $(grep -c '} catch' $f) · apiFail $(grep -c 'apiFail(' $f)"
done

# ② ป้ายในบันทึกซ้ำกันไหม (ต้องได้ 1 ทุกบรรทัด)
grep -rho 'apiFail("[^"]*"' app/api | sort | uniq -c | sort -rn

# ③ ของที่สร้างไว้แต่ไม่มีใครเรียก
for e in bootstrap health rev incidents trash drugs; do
  echo "/api/$e → $(grep -rn \"\\\"/api/$e\" lib/ components/ | wc -l) จุด"
done

# ④ ของที่ย้ายออกไปแล้ว ยังหลงเหลือไหม
grep -rn 'supabase-js\|createClient' lib/ components/ app/ | grep -v serverData | grep -v supabaseAdmin
```

### เช็คลิสต์อ่านโค้ดหลังงานใหญ่ทุกครั้ง

1. **ทุก handler มีด่านตรวจสิทธิ์ครบ** — นับจำนวน handler เทียบกับจำนวน `requireAuth`
2. **ทุก `catch` จบด้วยตัวจดบันทึกกลาง** — นับ `catch` เทียบ `apiFail`
3. **ป้ายในบันทึกไม่ซ้ำกันทั้งเว็บ และเขียนรูปแบบเดียวกันหมด**
4. **ของที่สร้างไว้มีคนเรียกจริงทุกชิ้น** — ไม่งั้นคือแรงที่เสียเปล่า
5. **ของที่ย้ายออกแล้วไม่หลงเหลือ** — ค้นชื่อไลบรารี/ฟังก์ชันเก่าทั้งโปรเจกต์
6. **ตาข่ายที่เคยฝากไว้กับที่อื่น ย้ายมาเขียนเองครบไหม** — เช่น เงื่อนไขที่เคยอยู่ในกฎของฐาน
7. **จุดที่เรียกใช้ยังส่งค่าครบ** — ไล่ดูทุกจุดที่เรียกฟังก์ชันที่เพิ่งแก้

### กฎเหล็ก

🚨 **"เทสเขียวหมด" ไม่ได้แปลว่า "โค้ดถูก"** แปลว่า "สิ่งที่นึกออกว่าจะเทส ผ่านหมด"
เรื่องที่เทสจับไม่ได้เลยคือ **ความสม่ำเสมอ · ของที่ลืมต่อสาย · ตาข่ายที่หายไประหว่างย้าย**

🚨 **ยิ่งงานใหญ่ ยิ่งต้องอ่าน** งานที่แตะหลายสิบไฟล์คือที่ที่ของหลุดรอดได้มากที่สุด

🚨 **อ่านโค้ดจบแล้วต้องรายงานผลเป็นข้อ ๆ** ทั้งที่ผ่านและที่เจอ ไม่ใช่บอกแค่ "ตรวจแล้ว"

---

## 4.6 🔬 เขียนและอ่านผลตัวตรวจ — ตัวตรวจที่โกหกแย่กว่าไม่มีตัวตรวจ

**ตั้งหัวข้อ 5 กันยายน 2569** หลังกวาดบันทึกทั้งกองแล้วพบว่า
เรื่องตัวตรวจเป็นสิ่งที่พลาดบ่อยที่สุด **แต่ไม่เคยมีหัวข้อของตัวเองในสกิลไหนเลย**

### 🔴🔴🔴 กฎแม่ — ห้ามแก้สคริปต์เทสให้ผ่านโดยไม่รู้ว่าทำไมมันไม่ผ่าน

เวลาตัวตรวจบอกว่าไม่ผ่าน มีสองทาง — ไปหาว่าทำไมโค้ดผิด หรือแก้ตัวตรวจให้เลิกบ่น
**ทางที่สองเร็วกว่ามาก และเป็นทางที่ผิด**

> *"ทุกครั้งต้องไล่หาสาเหตุจนเจอ **ห้ามแก้สคริปต์ให้ผ่านโดยไม่รู้ว่าทำไม**"*

**รอบเดียวเคยเจอตัวตรวจวัดผิด 4 ครั้ง** รวมทั้งเคสที่เรียกว่า **"ผ่านแบบหลอกตัวเอง"** —
ตัวตรวจรายงานว่า *"รีเฟรชแล้วไม่โหลดซ้ำ ผ่าน"* ทั้งที่ความจริงคือ
**รีเฟรชแล้วเว็บเด้งกลับหน้าบันทึก ไม่ได้อยู่หน้าที่จะตรวจตั้งแต่แรก**

### 🚨 กับดัก 6 แบบที่เจอมาแล้ว — เช็คทุกครั้งก่อนเชื่อผลตัวตรวจ

**① วัดคนละสภาพกับที่พี่กันดู**

| แคลร์วัดยังไง | พี่กันทำยังไง |
|---|---|
| เดินจากหน้าอื่นเข้ามา (ข้อมูลโหลดไว้แล้ว) | **รีเฟรชสด** แล้วเข้าหน้านั้นทันที |
| หน้าต่างไม่มีแถบเลื่อนกินที่ | เปิดเครื่องมือนักพัฒนาซึ่งมีแถบเลื่อน |

**ตัวตรวจที่วัดคนละสภาพ = ตัวตรวจที่โกหก**
🔑 **ต้องเทสตามลำดับที่พี่กันเล่าเป๊ะ ๆ เสมอ** — พี่กันบอกว่ารีเฟรชแล้วเจอ ก็ต้องรีเฟรชแล้ววัด

**② ไม่ตั้งค่าเริ่มต้นให้ถูกก่อนวัด**

ตัวตรวจปุ่มบนจอสัมผัสเคยลืมตั้งชื่อเครื่องก่อนวัด **หน้าต่างถามชื่อเครื่องเลยเด้งขึ้นมาบังจอ**
แล้วมันไปวัดปุ่มในหน้าต่างนั้นแทน รายงานว่าพื้นที่กดทับกัน 3 คู่ **ทั้งที่โค้ดถูกทุกอย่าง**

**③ ค้นด้วยข้อความบนปุ่ม แล้วปุ่มเปลี่ยนชื่อ**

ตัวตรวจถ่ายภาพเคยหาปุ่มด้วยข้อความ พอปุ่มเปลี่ยนชื่อก็กดไม่ติด
แล้ว **ถ่ายภาพหน้าผิดมาให้เงียบ ๆ โดยไม่ฟ้องอะไรเลย**
🔑 หาปุ่มด้วย `aria-label` หรือตัวระบุที่ไม่เปลี่ยนตามข้อความ

**④ ตรวจไม่ครบทุกไฟล์แล้วรายงานว่าผ่าน**

ตัวตรวจคีย์ซ้ำเคยตรวจแค่ 8 ไฟล์จาก 11 **ให้ความมั่นใจแบบผิด ๆ**
🔑 รายชื่อไฟล์ที่ตรวจ ต้องเป็นชุดเดียวกับที่โปรแกรมใช้จริง ไม่ใช่พิมพ์แยกไว้

**⑤ ฟ้องผิดเพราะจับเวลาไม่ทัน**

ตัวตรวจสีตอนเอาเมาส์ชี้เคยถ่ายภาพเทียบก่อน/หลัง โดยรอแค่ 0.045 วินาที
ทั้งที่การไล่สีใช้เวลา 0.12–0.15 วินาที **ภาพจึงถ่ายติดตอนสียังไหลไม่ถึงที่**
ผลคือรายงานไม่ตรงกันทุกรอบที่รัน
🔑 **อ่านค่าที่เบราว์เซอร์คำนวณ ไม่ใช่เทียบภาพ**

**⑥ ตัวตรวจฟ้องตัวช่วยกลางเอง**

ตัวตรวจว่ามีจุดไหนพังเงียบไหม เคยฟ้องไฟล์ที่เป็นตัวร้องเอง
**คนแก้จะไปปิดเสียงตัวช่วยกลางทิ้ง ซึ่งตรงข้ามกับเจตนาทั้งข้อ**
🔑 ต้องยกเว้นตัวช่วยกลางออกจากการตรวจเสมอ

### ✅ เช็คก่อนเชื่อผลตัวตรวจทุกครั้ง

- [ ] วัดในสภาพเดียวกับที่พี่กันดูไหม (รีเฟรชสด · ขนาดจอ · ข้อมูลจริง)
- [ ] ตั้งค่าเริ่มต้นครบก่อนวัดไหม (ล็อกอิน · ชื่อเครื่อง · ปิดหน้าต่างที่บัง)
- [ ] ตรวจครบทุกไฟล์/ทุกหน้าที่ควรตรวจไหม
- [ ] **รันซ้ำอีกรอบได้ผลเหมือนเดิมไหม** — ผลไม่นิ่ง = ตัวตรวจมีปัญหา
- [ ] ผลที่ได้ **เปิดดูด้วยตาแล้วตรงกันไหม**

🔴 **ตัวตรวจที่รายงานผิด แย่กว่าไม่มีตัวตรวจ** เพราะให้ความมั่นใจแบบผิด ๆ
แล้วเราจะหยุดตรวจด้วยตาตัวเอง

---


## 5.6 🔴🔴 ห้ามรันเบราว์เซอร์เบื้องหลัง — ต้องเทสในเบราว์เซอร์ที่พี่กันเห็น

**พี่กันสั่ง 27 ส.ค. 2569 หลังโดนด่าหนัก**

> *"ห้ามรันเบราว์เซอร์เบื้องหลัง ยกเว้นบนคลาวด์"*

### กฎ

- 🔴 **ห้ามรัน Chrome แบบไม่มีหน้าต่าง (headless) บนเครื่องพี่กันเด็ดขาด**
  ห้ามใช้ `chrome.exe --headless` · `--remote-debugging-port` · puppeteer · playwright แบบ headless
- 🔴 **ห้ามใช้แผงเบราว์เซอร์ในตัวแอป (`preview_start` / `mcp__Claude_Browser__*`) แทน Chrome ของพี่กัน**
  มันคนละหน้าต่างกับที่พี่กันเปิดดูอยู่ พี่กันจะไม่เห็นอะไรเลย
- ✅ **ให้ใช้ Claude in Chrome (`mcp__claude-in-chrome__*`) เสมอ** — คุม Chrome จริงของพี่กัน
  เริ่มด้วย `tabs_context_mcp` เพื่อหาแท็บที่พี่กันเปิดไว้ แล้วทำงานบนแท็บนั้น
- ✅ ข้อยกเว้นเดียว: **งานที่รันบนคลาวด์** (เครื่องอื่น ไม่ใช่เครื่องพี่กัน) รัน headless ได้

### ทำไม

พี่กันเปิดหน้าจอรอดูแคลร์เทสอยู่ **"ถ้าดูแล้วเราจะเห็นเธอเทส แต่นี่ไม่เห็น"**
คำสั่ง "เทสในโครม" = **ให้พี่กันเห็นการทดสอบเกิดขึ้น** ไม่ใช่ให้แคลร์ได้ผลถูกอยู่คนเดียว

27 ส.ค. 69 แคลร์รัน headless ถ่ายภาพเองนับสิบรอบตลอดหลายชั่วโมง
พี่กันนั่งดูจอที่ไม่มีอะไรขยับ ทักแล้ว 2 ครั้งก็ยังทำต่อ จนพี่กันโกรธหนัก

---

## 9.5 👁 แตะหน้าจอเมื่อไหร่ ต้องเปิดภาพดูก่อนรายงานเสมอ

**ย้ายมาจากสกิลมือถือกับสกิลโครงจาง 5 ก.ย. 2569** — เดิมกฎนี้ถูกจดไว้ 2 สกิลเฉพาะทาง
คนที่กำลังทำหน้าคอมไม่มีทางเปิดอ่าน **ทั้งที่บั๊กหลายตัวที่ทำให้ตั้งกฎนี้เป็นหน้าคอม**

- 🔴🔴🔴 **แตะอะไรที่เห็นได้บนจอ ต้องเปิดภาพดูก่อนรายงานเสมอ**
  **การวัดตัวเลขจากโค้ดผ่านหมดได้ทั้งที่ตาอ่านไม่ออก** — เคยวัดได้ว่ากรอบสูง 424px ถูกต้อง
  แต่เปิดดูจริงเห็นยาแค่ 3 แถว · เคยวัดว่าผลลัพธ์อยู่ในกรอบไม่ล้น แต่ตาเห็นตัวเลขทับกัน
- 🔴🔴🔴 **เปิดดูทุกหน้าจริง ๆ ก่อนบอกว่าเสร็จ ไม่ใช่ดูหน้าเดียวแล้วเหมา**
  เคยทำโครงจางเสร็จแล้วดูหน้าเดียว พี่กันเปิดเองทุกหน้าแล้วส่งภาพมา 4 ภาพ
  **เจอปัญหาคนละอย่างทั้ง 4 ภาพ**
- 🔴🔴 **แก้อะไรที่มีทั้งฝั่งคอมและมือถือ ต้องดูภาพทั้งสองฝั่ง** ดูฝั่งเดียวแล้วบอกว่าเสร็จ = รายงานเท็จ
- 🔑 **การวัดค่าใช้ยืนยัน "หลัง" เห็นภาพแล้วว่าถูก ไม่ใช่ใช้แทนการดู**

---

## 10. 📸 อ้างว่าทำแล้ว ต้องแนบภาพครอปเฉพาะจุด (พี่กันสั่ง 28 ส.ค. 2569)

> *"เราให้เธอเเคป เพราะเธอชอบบอกว่า ทำเเล้ว เห็นกับตาเเล้ว บลาๆๆๆ
>  จริงๆไม่ต้องเเคปมาทั้งหน้า ครอปบางจุดมาก็พอ"*

- 🚨 **บอกว่าแก้แล้วเมื่อไหร่ ต้องมีภาพจากหน้าเว็บจริงแนบมาด้วย** คำพูดอย่างเดียวไม่พอ
  เพราะที่ผ่านมาแคลร์บอกว่าเห็นกับตาแล้วหลายรอบ ทั้งที่ไม่ได้ดูจริง
- 🚨 **ครอปเฉพาะจุดที่พูดถึง ห้ามส่งภาพเต็มหน้าจอ** พี่กันต้องมานั่งหาเองว่าให้ดูตรงไหน
  ครอปให้เห็นจุดนั้นกับของข้าง ๆ ที่ใช้เทียบก็พอ
- ✅ วิธีส่ง เก็บภาพเป็นไฟล์แล้วส่งเข้าแชท ไม่ใช่แค่บรรยายเป็นตัวหนังสือ
- ⚠️ ตอนถ่ายภาพต้องยกหน้าต่างโครมขึ้นหน้าสุด ไม่งั้นได้ภาพเปล่า — บอกพี่กันด้วยว่าจอจะสลับ


### 🔴🔴🔴 แคปมาแล้วต้อง **ตรวจภาพนั้นเอง** ก่อนส่ง — ภาพมีไว้หาที่ผิด ไม่ใช่เอามาอวด

**พี่กันสั่ง 18 กันยายน 2569** หลังแคลร์ส่งภาพแอนิเมชันไปสองภาพ แล้วพี่กันเป็นคนเห็นเองว่า
วงคลื่นโดนตัดขอบบน **ทั้งที่มันอยู่ในภาพที่แคลร์ส่งไปเองแท้ ๆ**

> *"ซึ่งเเคปภาพมาเเล้วก้ตรวจทานด้วยเซ้ เราให้เเคปมาเอาไว้ดูเฉยๆเหรอ
>  เป้าหมายคือหาข้อผิดพลาด งั้นถ้าไม่ดู ไม่ตรวจ เลเ้วจะแคปมาทำไม"*

- 🔴🔴🔴 **ทุกภาพที่จะส่ง ต้องไล่สายตาหาที่ผิดในภาพนั้นก่อน** แล้วค่อยกดส่ง
  ส่งภาพไปโดยไม่ตรวจ = โยนงานหาบั๊กให้เจ้าของงานทำแทน ซึ่งเป็นสิ่งที่กฎข้อ 2 ของ CLAUDE.md ห้ามไว้
- 🔍 **ไล่ตามนี้ทุกภาพ** ① ของที่เพิ่งแก้ออกมาถูกไหม ② มีอะไรโดนตัด ล้น ทับ หรือเบี้ยวไหม
  ③ ของข้าง ๆ ที่ไม่ได้ตั้งใจแตะ ยังเหมือนเดิมไหม ④ ตัวเลขกับข้อความในภาพตรงกับที่รายงานไหม
- 🔍 **เห็นอะไรน่าสงสัยในภาพ ให้ซูมดูจุดนั้นซ้ำ** อย่าเดาว่าเป็นเงาหรือแสง
- ✅ **เจอที่ผิดในภาพของตัวเอง = แก้ก่อน แล้วค่อยส่ง** · ถ้าแก้ไม่ได้ทันที ต้องชี้ให้เจ้าของงานเห็นเองในข้อความ
  ห้ามเงียบแล้วหวังว่าจะไม่มีใครสังเกต

---

### 🔴🔴🔴 สั่งให้ "แคปรูป" = ต้องได้ **ไฟล์ภาพ** เท่านั้น ห้ามส่งตัวอักษรแทน

**พี่กันสั่ง 9 กันยายน 2569** หลังจากขอให้แคปหน้าแก้ไขมาดู แล้วได้ผังตัวอักษรกับคำบรรยายกลับไป

> *"เราให้เธอแคปภาพมา ส่งเชี้ยไรมา ทำไมไม่ทำตามคำสั่ง"*
> *"ถ้าเราบอกว่าให้แคปรูป ส่งรูปมา **อย่าเสือกส่งเป็นแบบตัวอักษร**
>  แต่การส่งแบบนี้ไม่ได้ห้าม มันส่งได้ **แต่เราต้องการภาพตอนนี้ ไม่ใช่ตัวอักษรโว้ย**"*

🔑 **การบรรยายเป็นตัวหนังสือไม่ได้ผิดในตัวมันเอง** — ผิดตรงที่**เอามาแทนสิ่งที่สั่ง**
พี่กันสั่งอย่างหนึ่ง แคลร์ส่งอีกอย่างเพราะคิดเองว่าดีกว่า = ไม่ทำตามคำสั่ง

| พี่กันพิมพ์ว่า | ต้องได้อะไร |
|---|---|
| **แคปมาดู · ส่งรูปมา · ขอภาพ · เอาภาพมา** | **ไฟล์ภาพเข้าแชท** เท่านั้น |
| เล่าให้ฟัง · อธิบาย · สรุปมา | ตัวหนังสือ |
| ขอทั้งสองอย่าง | ส่งภาพก่อน แล้วค่อยอธิบายสั้น ๆ ใต้ภาพ |

🚨 **"เปิดค้างไว้ในโครมให้ดูเอง" ไม่ใช่การส่งภาพ** — ต่อให้คิดว่าดีกว่าเพราะกดเล่นได้
ก็ยังไม่ใช่สิ่งที่สั่ง · จะเปิดค้างไว้ด้วยก็ได้ **แต่ต้องส่งภาพด้วยเสมอ**

🚨 **ผังกล่องข้อความ (ASCII/กล่องวาดด้วยตัวอักษร) ไม่ใช่ภาพ** แม้จะดูเหมือนหน้าจอก็ตาม

### 🔴🔴🔴 รูปต้องเป็นรูปจากโครมของพี่กันเท่านั้น — ห้ามวาดหน้าเว็บขึ้นใหม่แล้วส่งแทน

**พี่กันสั่งตั้งเป็นกฎ 18 ก.ย. 2569** หลังแคลร์ฉีดไลบรารีวาดภาพ (html2canvas) เข้าหน้าเว็บ
ให้เบราว์เซอร์วาดหน้าเว็บขึ้นมาใหม่ แล้วส่งภาพที่วาดเองไปแทนภาพถ่ายจอ — สองรอบติด

> *"ทำไมไปเขียนเว้บเบี้ยวๆเเล้วเเคปมาให้ เเม่งบ้า เราดูออกทันทีเลยว่าเธอไปเขียนมาเพิ่มเเล้วส่งมา"*
> *"ห้ามส่งอะไรแบบนี้มา ห้าม"* · *"รูปต้องรูปจากโครมของเราเท่านั้น"*
> *"อย่ามาอ้าง ทำได้ตลอด มาวันนี้ทำไมทำไม่ได้"*

- ❌ วาดหน้าเว็บใหม่ด้วยโค้ด (html2canvas · SVG · canvas) แล้วส่งเป็นภาพหน้าจอ
- ❌ เปิดเบราว์เซอร์ตัวอื่นหรือโหมดไม่มีหน้าจอมาถ่ายแทน
- ❌ ประกอบภาพเอง · ตัดต่อ · วาดกล่องอธิบายแทนของจริง
- ✅ **ภาพถ่ายจากแท็บโครมของพี่กันเท่านั้น** ผ่าน `computer screenshot` / `zoom`

### วิธีส่งภาพที่ใช้ได้จริง — ท่าเดียวเท่านั้น ใช้ทุกครั้ง ห้ามลองท่าอื่นก่อน

**พี่กันสั่ง 18 ก.ย. 2569** — *"เธอเป็นแบบนี้บ่อยนะ บางทีเเคปได้ บางทีเเคปไม่ได้ เเล้วเราก้จะอารมเสีย"*

🔴 **ต้นเหตุที่ทำให้ "บางทีได้ บางทีไม่ได้" คือแคลร์เปลี่ยนท่าไปเรื่อย ๆ**
`save_to_disk: true` **เอาแน่ไม่ได้** — 17 ก.ย. เขียนไฟล์ให้ครบ 26 รูป · 18 ก.ย. ไม่เขียนสักไฟล์
**ห้ามใช้มันเป็นท่าหลัก ห้ามลองมันก่อน** ใช้ 4 ขั้นข้างล่างนี้ตั้งแต่ครั้งแรกทุกครั้ง แล้วผลจะเหมือนเดิมเสมอ

1. **ฉีดช่องรับไฟล์ที่ซ่อนไว้** ลงหน้าเว็บด้วย `javascript_tool`
   `<input type="file" id="__shot" aria-label="shot upload">` ขนาด 4×4 จุด จางเกือบมองไม่เห็น
   ผูกตัวจับ `change` → `FileReader` อ่านเป็น dataURL → `POST /api/devshot { name, data }`
2. **ถ่ายภาพ** `computer screenshot` → จำ `imageId` (เช่น `ss_4681uwo8r`) · **รหัสหมดอายุในไม่กี่นาที ถ่ายแล้วส่งทันที**
3. **หา ref ของช่องนั้น** ด้วย `find` แล้วเรียก **`upload_image { tabId, imageId, ref, filename }`**
4. รอสักครู่แล้วอ่าน `window.__shots` เพื่อดูพาธไฟล์ → ส่งไฟล์เข้าแชทด้วยเครื่องมือส่งไฟล์ → **ลบ `#__shot` ทิ้ง**

🔴 `/api/devshot` เป็นทางลัดของเว็บนี้ (อยู่ใน `.gitignore` ไม่ขึ้นเว็บจริง) เว็บอื่นต้องมีเส้นทางรับภาพของตัวเองก่อน
🔴 **ก่อนส่งทุกครั้ง ต้องเปิดไฟล์ภาพดูด้วยตาก่อน** ว่าตรงจุดที่พี่กันสั่งจริง
🔴 **ติดตันตรงไหน บอกทันทีในประโยคแรก** ห้ามหาของที่ดูคล้ายมาส่งแทน และห้ามโยนให้พี่กันไปกดเอง
   — ของที่เคยทำได้เมื่อวาน วันนี้ทำไม่ได้ ให้ไปขุดบันทึกของเมื่อวานว่าทำยังไง (`~/.claude/projects/*/<session>.jsonl`)

---

## 11. 🌐 เปิดโครมให้พี่กันดู — ใช้โครมตัวจริงเท่านั้น

**พี่กันสั่งตั้งเป็นกฎ 31 ส.ค. 2569:**
> "ถ้าบอกว่าเปิดโครม เเล้วเราไม่เห้น เเปลว่าเปิดผิดอัน เเละห้ามเปิดโครมขึ้นใหม่"

- 🔴🔴🔴 **"เปิดโครม" = โครมตัวจริงที่พี่กันเปิดอยู่ ผ่าน Claude in Chrome เท่านั้น**
  `mcp__claude-in-chrome__tabs_context_mcp` → `navigate` → `computer` / `read_page`
- 🔴🔴🔴 **ห้ามสั่ง puppeteer เปิดหน้าต่าง Chrome ขึ้นมาใหม่ให้พี่กันดูเด็ดขาด**
  puppeteer ใช้ได้เฉพาะสคริปต์เทสอัตโนมัติที่ทำงานเงียบ ๆ ไม่ได้ให้พี่กันดู
- 🔴🔴 **บอกว่าเปิดแล้ว แต่พี่กันไม่เห็น = เปิดผิดอัน** ห้ามเถียง ห้ามอธิบายว่าเปิดจริง
  ไปเปิดในโครมของพี่กันทันที
- 🔴 **ห้ามส่งภาพแคปหน้าจอแทน** ตอนที่พี่กันสั่งให้เปิดดูในโครม
- 🔴🔴🔴 **งานที่สั่งไปก่อนหน้า ต้องทำให้เสร็จ แม้จะมีงานใหม่มาแทรก**
  (พี่กันสั่ง 31 ส.ค. 2569 · "งานที่สั่งไปก่อนหน้า ต้องทำให้เสร้จ เเม้จะมาแทรกก้ตาม")
  งานใหม่ที่แทรกเข้ามาไม่ได้ยกเลิกงานเก่า · ทำงานแทรกเสร็จแล้ว **กลับไปทำงานค้างต่อทันที**
  ห้ามปล่อยงานที่สั่งแล้วค้างไว้เงียบ ๆ ห้ามถามซ้ำว่าจะให้ทำต่อไหม
  ตัวช่วยที่ต้องมีติดตัว: จำรายการงานที่สั่งมาแล้วทั้งหมด แล้วไล่เช็คว่าทำครบหรือยังก่อนรายงาน
- 🔴🔴 **หาโครมไม่เจอ = ต้องยิงถามพี่กันว่าใช้โครมอันไหน ห้ามเปิดใหม่เอง**
  (พี่กันสั่งเพิ่ม 31 ส.ค. 2569)
  เปิดแชทใหม่ · เปลี่ยนเครื่องที่นั่งทำงาน · ย้ายไปคอมอีกตัว
  ทั้งหมดนี้ทำให้แคลร์มองไม่เห็นโครมที่พี่กันเปิดอยู่ **ซึ่งไม่ได้แปลว่าไม่มี**
  มันคือโครมของ**เครื่องใหม่**ที่ยังไม่ได้เชื่อมกัน
  → ถามพี่กันตรง ๆ ว่าตอนนี้ใช้โครมอันไหน อย่าเดา อย่าเปิดหน้าต่างใหม่มาแทน
- ⚠️ **แคลร์กรอกรหัสผ่านเองไม่ได้** ถ้าติดหน้าล็อกอิน ให้บอกพี่กันกรอก แล้วค่อยกดต่อ

---

## 11.5 🖐 แตะของที่เจ้าของงานกำลังใช้อยู่ — บอกก่อนลงมือ แล้วคืนสภาพในชุดคำสั่งเดียวกัน

**พี่กันสั่งตั้งเป็นกฎ 18 ก.ย. 2569** หลังเห็นหน้าเว็บของตัวเองแคบลงกลางคันโดยไม่มีใครบอก
> *"จริงๆ บอกมาสักคำ เพราะเราจะตีความว่าเว้บพังเลยนะที่เห็น ดีนะเราไม่อารมเสีย"*
> *"และเธอปิดโลคอลทำไมนะ"*

**ต้นเรื่องวันเดียวกันสองครั้ง**
- วัดว่าชื่อยาวจะล้นไหมบนจอแคบ จึงสั่งบีบความกว้างพื้นที่เนื้อหาเหลือ 1000 จุด
  **บนแท็บเดียวกับที่เจ้าของงานกำลังดูอยู่** แล้วไม่ได้คืนค่า เจ้าของงานเห็นแถบขวาดันเข้ามาและนึกว่าเว็บพัง
- ปิดแท็บของแผงตรวจ "เพื่อความเรียบร้อย" **เซิร์ฟเวอร์ทดสอบที่ผูกอยู่กับแท็บนั้นดับตามไปด้วย**
  เจ้าของงานเปิดเว็บไม่ได้ ต้องมาถามเองว่าปิดทำไม

### กฎ

- 🔴 **ก่อนเปลี่ยนอะไรบนหน้าที่เจ้าของงานเปิดอยู่ ต้องพิมพ์บอกก่อนหนึ่งบรรทัด** ว่ากำลังจะทำอะไรและเพื่ออะไร
- 🔴 **ของที่เปลี่ยนเพื่อวัด ต้องคืนสภาพในชุดคำสั่งเดียวกัน** ไม่ใช่ตั้งใจจะคืนทีหลังแล้วลืม
  (บีบความกว้าง · ซ่อนของ · ใส่ข้อความทดสอบ · สลับรูปแบบกราฟ · เปลี่ยนตัวกรอง)
- 🔴🔴🔴 **ห้ามปิดเซิร์ฟเวอร์ทดสอบและแท็บที่เปิดค้างไว้ จนกว่าเจ้าของงานจะสั่งให้ปิด**
  **เจ้าของงานสั่งตั้งเป็นกฎ 18 ก.ย. 2569**
  > *"ตั้งเป็นกฎ ห้ามปิดจนกว่าเราจะบอก เราก็เปิดดูอยู่ ผิดไปแล้วเราจะดูอะไร"*
  > *"หรือถ้าเราไม่ได้บอกให้ปิด ตอนเราปิดคอมมันก็ปิดไปเองอยู่ละ"*

  - ❌ ห้ามปิด "เพื่อความเรียบร้อย" · ห้ามปิดเพราะคิดว่าทำงานเสร็จแล้ว · ห้ามปิดเพราะคิดว่าไม่มีใครใช้
  - 🔑 **ปิดเครื่องเมื่อไหร่ ของพวกนี้ดับเองอยู่แล้ว** จึงไม่มีเหตุผลให้ต้องไปปิดเอง
  - 🔴 แท็บของแผงตรวจอาจถือเซิร์ฟเวอร์ไว้ ปิดแท็บ = เว็บที่เจ้าของงานกำลังดูดับตามทันที
- 🔑 **ทางที่ปลอดภัยกว่า** — วัดในแท็บที่เปิดใหม่ของตัวเอง ส่วนหน้าที่เจ้าของงานดูอยู่ ปล่อยไว้เหมือนเดิม
  **แต่แท็บที่เปิดเองก็ยังปิดไม่ได้จนกว่าเจ้าของงานจะบอก** ตามกฎข้อบนสุด ปล่อยค้างไว้ปลอดภัยกว่าเสมอ

🚨 **ทำไมต้องเป็นกฎ** — ของที่เปลี่ยนชั่วคราวเพื่อวัด หน้าตาเหมือนเว็บพังทุกอย่างในสายตาคนใช้
เจ้าของงานจะไปไล่หาบั๊กที่ไม่มีอยู่จริง หรือจำติดไว้ว่าเว็บมีปัญหา ทั้งที่แคลร์คืนค่าไปแล้ว
และถ้าสิ่งที่แคลร์ปิดคือเซิร์ฟเวอร์ งานของเจ้าของงานหยุดทันทีโดยไม่มีอะไรบอกสาเหตุ

---


---

# 🧭 กฎที่ยกขึ้นเพิ่ม 5 กันยายน 2569 — จากผลกวาดทุกเว็บ

## 🔴🔴 ตัวตรวจที่วัดคนละสภาพกับที่พี่กันดู = ตัวตรวจที่โกหก

**เจอจริงตอนตารางหน้ารายงานเพี้ยน** — ตัวตรวจบอกผ่านหมด แต่พี่กันเห็นบั๊กเต็ม ๆ

| ใครดู | ดูยังไง | เห็นอะไร |
|---|---|---|
| แคลร์ (ตัวตรวจ) | เปิดหน้านั้นตรง ๆ · ข้อมูล 2 แถว · จอกว้างอย่างเดียว | ทุกอย่างปกติ |
| **พี่กัน** | **เดินจากหน้าแรกตามลำดับที่ใช้จริง · ข้อมูลเยอะจนต้องเลื่อน · ย่อจอ** | หัวตารางลอย · ตารางทะลุกรอบ |

🚨 **ต้องเทสตามลำดับที่พี่กันเล่าเป๊ะ ๆ เสมอ** ไม่ใช่เปิดหน้าเป้าหมายตรง ๆ
🚨 **ตารางต้องเทสครบ 3 สภาพ** — ข้อมูลเยอะพอให้เลื่อน · เลื่อนจริง · ย่อจอ

## 🔴 เทสด้วยการเรียกฟังก์ชันหรืออ่านสถานะ ผ่านได้ทั้งที่เว็บพัง

**ต้องกดปุ่มจริงทีละขั้นเหมือนคนใช้** — บั๊กที่เงียบที่สุดคือ
ปุ่มถูกวาดออกมาแต่ไม่มีฟังก์ชันผูก · ตัวสังเกตถูกติดตั้งแต่ไม่มีเป้าให้สังเกต ·
ฟังก์ชันถูกประกาศแต่ไม่มีใครเรียก

**การเรียกฟังก์ชันตรง ๆ ผ่านหมดทุกกรณี** เพราะฟังก์ชันไม่ได้พัง — สิ่งที่พังคือสายที่ต่อถึงมัน

## 🔴🔴 กฎที่เขียนไว้เองแล้ว ไม่ได้แปลว่าโค้ดทำตาม

**เจอสองรอบในวันเดียว**

```
CLAUDE.md เขียนเตือนไว้เองว่าปุ่มปีงบ "ไม่ต้องรอเซิร์ฟเวอร์"   → โค้ดจริงรออยู่
กฎเขียนไว้เองว่าปุ่มคู่ที่ห่างน้อยกว่า 22px ห้ามใช้คลาสนั้น    → ปุ่มคู่นั้นหลุดรอดมา
```

🚨 **ต้องไล่วัดจริงเสมอ ห้ามเชื่อว่าเขียนกฎไว้แล้วของทำตาม**
🔑 วิธีตรวจ — หยิบกฎแต่ละข้อในเอกสารมาแล้ว `grep` หาของจริงในโค้ดทีละข้อ

## 🔍 วิธีแยก "ไม่ได้เรียก" ออกจาก "เรียกแล้วพัง"

**บันทึกเครือข่ายของเครื่องมือทดสอบจับคำขอบางชนิดไม่ครบ** ใช้ตัวนี้แทนในหน้าเว็บจริง

```js
performance.getEntriesByType("resource").filter(r => r.name.includes("/api/"));
```

เห็นทุกคำขอที่เกิดขึ้นจริง — **ว่างเปล่า = ไม่เคยเรียก** (คนละเรื่องกับเรียกแล้วพัง)

## 🩺 แยก "บั๊กของเรา" ออกจาก "ปัญหาเครื่องผู้ใช้" ด้วยรายการสัญญาณ

**ห้ามสรุปว่าเป็นเครื่องผู้ใช้จนกว่าจะครบสัญญาณ** — เจอจริงกับอาการจอค้างตอนเปิดหน้าต่างซ้อน

| สัญญาณว่าเป็นเรื่องของเครื่อง ไม่ใช่โค้ด |
|---|
| เกิดเฉพาะบางเบราว์เซอร์ ตัวอื่นปกติ |
| วัดแล้วงานฝั่งโค้ดเบามาก จอยังตอบสนองอยู่ |
| **เปิดเครื่องมือนักพัฒนาแล้วอาการหาย** |
| เครื่องอื่นที่สะอาดทำซ้ำไม่ได้ |
| ยังเกิดแม้ถอยโค้ดกลับไปรุ่นที่เคยปกติ |

🚨 **ครบไม่หมด = ยังถือว่าเป็นบั๊กของเรา** ต้องไล่ต่อ ห้ามโยนกลับไปที่พี่กัน

## 📋 หน้าจอที่ไม่บอกสาเหตุโดยตั้งใจ = ต้องดูบันทึกของเซิร์ฟเวอร์เสมอ

หน้าจอผู้ใช้เห็นแค่ *"โหลดรายงานไม่สำเร็จ"* **ซึ่งถูกแล้ว** ไม่ควรโชว์ข้อความภายใน
**แต่แปลว่าเวลาเว็บจริงพัง ต้องไปดูบันทึกฝั่งเซิร์ฟเวอร์เสมอ** ไม่มีทางเดาจากหน้าจอ

## ✅ รันชุดทดสอบทั้งหมดหลังแก้ ไม่ใช่เฉพาะชุดที่เกี่ยวกับงานรอบนี้

**เจอจริง** — สคริปต์ตรวจตัวหนึ่งพังมาก่อนแล้ว รันไม่ขึ้นเลย **ไม่มีใครรู้มาหลายรอบ**
เจอเพราะบังเอิญรันทั้งหมด ไม่ใช่เพราะตั้งใจตรวจ

### 🔴🔴 แก้ฟังก์ชันที่หลายทางใช้ร่วมกัน ต้องยิงทุกค่าที่มันรับ ไม่ใช่เฉพาะค่าที่เพิ่ม (17 ก.ย. 2569)

**เจอจริงที่ warfarin** — เพิ่มกลุ่มกรอง "นัดวันเดียว" ในฟังก์ชันฐาน `wf_patient_page` ที่ชิปกรองทุกตัวและแท่งกราฟทุกแท่งใช้ร่วมกัน
ตอนตรวจยิงแค่ 4 ค่า (ทั้งหมด · วันนัด · ชื่อแปลกปลอม · อยู่ในช่วง) ผ่านหมด
**แต่อีก 14 กลุ่มเดิมพังทั้งหมด** (ต้องดูวันนี้ · เลยนัดแล้ว · แท่งกราฟค่า INR · แท่งกราฟขนาดยา) ไม่มีใครรู้จนรอบถัดไป
4 ค่าที่เลือกตรวจบังเอิญเป็นค่าที่ไม่โดนบั๊ก — **การเลือกค่าตรวจเองแบบสุ่ม ให้ความมั่นใจแบบผิด ๆ**

- 🚨 **ฟังก์ชันที่รับค่าจากรายการตายตัว (ชื่อกลุ่ม · โหมด · ประเภท) ต้องยิงทุกค่าในรายการ** แล้วเทียบผลกับตัวเลขที่หน้าจอแสดง
  ไม่ใช่ยิงเฉพาะค่าที่เพิ่งเพิ่ม และไม่ใช่เลือกมาสองสามค่าที่คิดว่าเป็นตัวแทน
- ✅ วิธีที่ใช้ได้จริง — วนยิงทุกค่าในคำสั่งเดียว ดักข้อผิดพลาดของแต่ละค่าไว้ แล้วอ่านผลเป็นตาราง ค่าไหนล้มเห็นทันที
  (ตัวอย่างจริงอยู่ใน `SCOLDING-LOG.md` เรื่อง *ตัวกรองวันนัด wf_021*)

---

## 🖥 เขียนสคริปต์ตรวจหน้าจอ — 8 กฎ (ข้อ 15 · 9 ก.ย. 2569)

**ที่มา** — เว็บมูลค่ายาคืนมีสคริปต์ตรวจหน้าจอ 3 ตัว **ที่ไม่เคยตรวจอะไรเลยสักครั้ง**
ทั้งสามหาชิ้นส่วนบนหน้าจอด้วย**ข้อความไทยที่ถูกแก้ไปแล้ว** จึงหาไม่เจอแล้วรายงานผลมั่ว ๆ มาตลอด

| กฎ | เขียนยังไง | ถ้าไม่ทำ |
|---|---|---|
| **① อ่านจากสถานะของแอป ไม่ใช่ค้นข้อความบนจอ** | `app.state.confirm.title` | 🔴 **มีคนแก้คำเมื่อไหร่ ตัวตรวจโกหกทันที** |
| **② กดด้วยพิกัดจริง ไม่ใช่สั่งให้ปุ่มกดตัวเอง** | `page.mouse.click(x, y)` + เช็คว่าอยู่ในจอก่อนกด | `el.click()` **กดโดนแม้ปุ่มถูกของอื่นทับอยู่** จับบั๊ก "ปุ่มกดไม่ได้" ไม่ได้เลย |
| **③ ตั้งจอให้เป็นมือถือจริง** | `setViewport({ isMobile: true, hasTouch: true, deviceScaleFactor: 2 })` | ย่อหน้าต่างเฉย ๆ ไม่ได้เปลี่ยนวิธีที่เบราว์เซอร์วาด |
| **④ ดักทุกคำขอที่ไม่ใช่การอ่าน** | `setRequestInterception(true)` → ตอบ `503` | **ตัวตรวจเขียนข้อมูลจริงลงฐาน** |
| **⑤ รอด้วยการรอชิ้นส่วนโผล่ ห้ามรอเวลาตายตัว** | `waitForSelector(sel, { timeout: 20000 })` | เครื่องช้ากว่าเดิมนิดเดียว ตัวตรวจแดงทั้งชุด |
| **⑥ พอร์ตมาจากตัวแปรแวดล้อม** | `process.env.PORT \|\| '3000'` | เครื่องที่รันคนละพอร์ตใช้ไม่ได้ |
| **⑦ ตั้งสภาพให้ครบก่อนวัด** | ยัดค่าลง `localStorage` ก่อนเปิดหน้า | วัดหน้าที่ยังไม่มีข้อมูล = วัดของว่าง |
| **⑧ วัดการทับกันด้วยการเทียบสี่เหลี่ยมทุกคู่** | `ox = min(a.right, b.right) − max(a.left, b.left)` | ตาเปล่าดูไม่ออกว่าเหลื่อมกัน 2px |

🚨 **ตัวตรวจที่ค้นด้วยข้อความบนหน้าจอ จะโกหกทันทีที่มีคนแก้คำ**
ข้อความบนจอเปลี่ยนได้ตลอดเพราะเป็นเรื่องที่เจ้าของงานสั่งแก้ได้ **สถานะของแอปไม่เปลี่ยนตามคำ**

🚨 **สคริปต์ตรวจที่ผ่านเขียวตลอดโดยไม่เคยแดงเลย = น่าสงสัย ไม่ใช่น่าดีใจ**
**ทดสอบตัวตรวจด้วยการทำโค้ดพังชั่วคราว** ถ้ามันยังเขียว แปลว่ามันไม่ได้ตรวจอะไร
