---
name: search-system
description: ใช้ทุกครั้งที่ทำหรือแก้ช่องค้นหาและระบบค้นหาในเว็บ — ตั้งแต่สมองการค้นหา (จัดอันดับ พิมพ์หลายคำ ลืมสลับแป้นพิมพ์ ค้นด้วยตัวย่อ) หน้าตาช่องค้นหา (ไอคอนแว่นขยาย ปุ่มล้างคำค้น ป้ายบอกคำที่ใช้ค้นจริง วรรณยุกต์ไทยไม่โดนตัด) ไปจนถึงผลค้นหาบนจอ (ไฮไลต์คำที่เจอ สีความแรง ลูกศรเลื่อน) พร้อมค่าจริงทุกตัวที่ยกไปใช้ได้เลย และคำสั่งตรวจว่าทำครบจริงไหม
---

# ระบบค้นหา — กฎกลาง ใช้ได้ทุกโปรเจกต์

> ตั้งขึ้น **5 กันยายน 2569** ตามที่พี่กันสั่ง
> *"แยกระบบค้นหาทั้งหมดออกมาเลย เพราะระบบค้นหาเรามีไรมั่ง เยอะเลย
>  ทั้งค้นสียาเปลี่ยนเอย ค้นโดยพิมโดยไม่เปลี่ยนภาษา ปุ่ม x ยกเลิกการพิมเอย และอื่นๆ"*

**ทำไมแยกออกมา** — กฎเรื่องค้นหาเคยกระจายอยู่ 2 สกิล (`working-with-gun` ข้อ 9.x กับ
`pharmacy-web-logic` ข้อ 10-16 · 25 · 28 · 32 · 33) ซึ่งเป็นสกิลยาวเกิน 1,300 บรรทัดทั้งคู่
**ท้ายไฟล์ถูกตัดตอนบทสนทนายาว** กฎที่เขียนไว้จึงมีโอกาสไม่ถูกอ่านสูงมาก
5 ก.ย. 2569 พี่กันจับได้ว่า ME-DRP ไม่ทำตามกฎช่องค้นหาเลยสักข้อ ทั้งที่กฎมีอยู่ครบแล้ว

---

# 🌳 มีอะไรบ้าง

```
ระบบค้นหา
│
├─ 🧠 สมองการค้นหา ──────────── lib/drugSearch.js (ยกไปทั้งไฟล์ได้)
│  ├─ 1. ลำดับการจัดอันดับ 7 ชั้น        ← เรียงผิด = ยาหายจากผลค้นหา
│  ├─ 2. พิมพ์หลายคำ "amox clav"
│  ├─ 3. ลืมสลับแป้นพิมพ์ ทำะดนพทรื → metformin
│  ├─ 4. ค้นด้วยชื่อการค้า และตัวย่อ
│  ├─ 5. ของที่ใช้บ่อยขึ้นก่อน
│  └─ 6. จัดกลุ่มตามรูปแบบยา
│
├─ 🔍 หน้าตาช่องค้นหา ────────── ตัวกลางตัวเดียว ห้ามวาดเอง
│  ├─ 7. เรียกตัวกลาง ห้ามวาดเอง         ← ทุกช่องต้องหน้าตาเดียวกัน
│  ├─ 8. ไอคอนแว่นขยาย
│  ├─ 9. ปุ่ม ✕ ล้างคำค้น
│  ├─ 10. ป้าย "ค้นว่า ..."
│  ├─ 11. วรรณยุกต์ไทยห้ามโดนตัด        ← 🔴 อันตรายสุด
│  └─ 12. ตัวอักษรนั่งกลางกรอบ
│
├─ 🎨 ผลค้นหาบนจอ ─────────────
│  ├─ 13. ไฮไลต์คำที่ค้นเจอ
│  ├─ 14. สีความแรงตอนชื่อซ้ำ
│  ├─ 15. ลูกศรเลื่อน กรอบเลื่อนตาม
│  ├─ 16. ลูกศรวนรอบ
│  └─ 17. กรอบผลค้นกันหน้าเพจไหลตาม
│
└─ ✅ วิธีตรวจว่าทำครบจริง
```

## 📊 ตารางเช็ค — เว็บไหนทำอะไรแล้ว

> ✅ ทำแล้ว · 🔨 ทำค้าง · ⬜ ยังไม่ทำ · ➖ ไม่เกี่ยว
> 🚨 **ห้ามเชื่อตารางนี้โดยไม่ตรวจโค้ดจริงก่อน** ตารางล้าสมัยได้เสมอ

| # | เรื่อง | ยาคืน | ME-DRP | เสียหายถ้าไม่ทำ |
|---|---|:---:|:---:|---|
| 1 | ลำดับการจัดอันดับ 7 ชั้น | ✅ | ✅ | 🔴 ยาหายจากผลค้นหาทั้งที่มีในคลัง |
| 2 | พิมพ์หลายคำ | ✅ | ✅ | 🟡 `amox clav` ไม่เจอ |
| 3 | ลืมสลับแป้นพิมพ์ | ✅ | ✅ | 🟡 ต้องลบพิมพ์ใหม่ทั้งคำ |
| 4 | ค้นด้วยชื่อการค้า/ตัวย่อ | ✅ | ✅ | 🟡 ค้นด้วยชื่อที่เรียกจริงไม่ได้ |
| 5 | ของที่ใช้บ่อยขึ้นก่อน | ✅ | ⬜ | 🟢 ต้องเลื่อนหา (พี่กันบอกยังไม่ต้อง) |
| 6 | จัดกลุ่มตามรูปแบบยา | ✅ | ✅ | 🟠 ยาชนิดเดียวกันกระจัดกระจาย |
| 7 | ช่องค้นหาเรียกตัวกลาง | ✅ | 🔨 | 🟠 ช่องหน้าตาคนละแบบ ต้องมองซ้ำทุกครั้ง |
| 8 | ไอคอนแว่นขยาย | ✅ | 🔨 | 🟡 ดูไม่ออกว่าช่องไหนคือช่องค้นหา |
| 9 | ปุ่ม ✕ ล้างคำค้น | ✅ | 🔨 | 🟡 ต้องลากคลุมแล้วลบเอง |
| 10 | ป้าย "ค้นว่า ..." | ✅ | 🔨 | 🟡 งงว่าพิมพ์ไทยแล้วทำไมเจอของอังกฤษ |
| 11 | วรรณยุกต์ไม่โดนตัด | ✅ | ⬜ | 🔴 **"นี้ นั้น ชื่อ" ไม้โทหายครึ่งตัว** |
| 12 | ตัวอักษรนั่งกลางกรอบ | ✅ | 🔨 | 🟡 ตัวหนังสือลอยสูงกว่าไอคอน |
| 13 | ไฮไลต์คำที่ค้นเจอ | ✅ | ✅ | 🟠 กวาดตาหาไม่เจอว่าตรงไหนตรงกับที่พิมพ์ |
| 14 | สีความแรงตอนชื่อซ้ำ | ✅ | ⬜ | 🔴 หยิบสลับความแรง |
| 15 | ลูกศรเลื่อน กรอบเลื่อนตาม | ✅ | ✅ | 🟠 กดเลือกแบบตาบอด |
| 16 | ลูกศรวนรอบ | ✅ | ✅ | 🟢 |
| 17 | กันหน้าเพจไหลตาม | ✅ | ✅ | 🟡 เลื่อนดูผลค้นแล้วหน้าหนี |

**🔨 ของ ME-DRP ข้อ 7-10 · 12** — สร้าง `components/SearchBox.tsx` ไว้แล้ว 241 บรรทัด
**แต่ยังไม่มีจุดไหนเรียกใช้** ช่องค้นหา 4 ช่องยังเป็น `<input>` ดิบ
(`DashboardView:551` · `DrugsAdminView:156` · `RecordsView:279` · `MedDrpApp:1045`)

---

# 🧠 ส่วนที่ 1 · สมองการค้นหา

**ต้นฉบับ:** `C:\Users\PKH\Returned-Drug-Value\lib\drugSearch.js` (277 บรรทัด)

เขียนเป็นโมดูลอิสระโดยตั้งใจ — ไม่พึ่ง React · ไม่พึ่ง Next.js · ไม่พึ่งไฟล์อื่น
**ก๊อปไปวางโปรเจกต์อื่นได้ทั้งไฟล์**

```js
import { searchDrugsEx, moveHi, markMatch, thaiToEnglish } from '@/lib/drugSearch';

const { list, used, swapped } = searchDrugsEx(drugs, q, {
  limit: 14,
  minLen: 2,
  hotIds: st.hotIds,     // รหัสของที่ใช้บ่อย
});
```

| คืนอะไรมา | ไว้ทำอะไร |
|---|---|
| `list` | ผลค้นหาที่จัดอันดับแล้ว |
| `used` | **คำที่ระบบใช้ค้นจริง** (หลังแปลงแป้นพิมพ์) — เอาไปไฮไลต์ ห้ามใช้คำที่ผู้ใช้พิมพ์ |
| `swapped` | จริงเมื่อระบบแปลงแป้นพิมพ์ให้ — เอาไปโชว์ป้าย "ค้นว่า ..." |

## 1. 🔴 ลำดับการจัดอันดับ 7 ชั้น — ห้ามสลับ

```
1) เจอที่ต้นชื่อ มาก่อนเจอกลางชื่อ
2) ตำแหน่งที่เจอ ยิ่งต้นยิ่งมาก่อน
3) ของที่ใช้บ่อย (hotIds)
4) ชื่อตามตัวอักษร          ← 🔴 ชั้นนี้คือตัวกันความแรงเรียงข้ามยา
5) รูปแบบยา (FORM_ORDER)
6) ความแรงน้อย → มาก        เฉพาะของชื่อเดียวกัน
7) ชื่อเต็ม
```

### 🔴 บั๊กที่เคยเจอจริง — พิมพ์ `met` แล้ว Metformin ไม่ขึ้นเลย

เพราะเรียงตามความแรงข้ามยาคนละตัว

```
0.2 → 2.5 → 5 → 10 → 12.5 → 100 → 200 │ 250 → 500 (Metformin)
                        ตัดที่ 8 ตัว ┘   ตกขอบ
```

**Metformin คือยาที่คืนบ่อยที่สุด** แต่หายจากผลค้นหาทั้งที่มีในคลัง

ชั้นที่ 6 ยังจำเป็น — พิมพ์ `enal` ต้องได้ **Enalapril 5 mg** ก่อน **20 mg**
(เรียงตัวอักษรล้วนจะได้ `"2"` มาก่อน `"5"` = 20 mg ขึ้นก่อน ซึ่งผิด)

🚨 **ตำแหน่งที่เจอถูกถ่วงตามที่มา** — ชื่อสามัญ `0+` · **ตัวย่อ `500+`** · ชื่อการค้า `900+`
ของที่ตรงกับชื่อจริงต้องมาก่อนของที่ตรงเพราะตัวย่อเสมอ

## 2. พิมพ์หลายคำ

```
"amox clav"  →  ["amox","clav"]  →  ชื่อไหนมีครบทุกคำ = เจอ
"clav amox"  →  สลับลำดับก็เจอ
```

```js
for (const t of tokens) {
  if (name.indexOf(t) < 0 && brand.indexOf(t) < 0) return -1;
}
```

Google · PubMed · Algolia · Elasticsearch ค้นแบบนี้กันหมด **คนคาดหวังแบบนี้อยู่แล้ว**

## 3. ลืมสลับแป้นพิมพ์

**อาการ** — ตั้งใจพิมพ์ `metformin` แต่แป้นค้างที่ไทย → ได้ `ทำะดนพทรื`

```js
const TH_TO_EN = { 'ๆ':'q','ไ':'w','ำ':'e','พ':'r','ะ':'t','ั':'y','ี':'u','ร':'i','น':'o','ย':'p',
  'ฟ':'a','ห':'s','ก':'d','ด':'f','เ':'g','้':'h','่':'j','า':'k','ส':'l','ว':';',
  'ผ':'z','ป':'x','แ':'c','อ':'v','ิ':'b','ื':'n','ท':'m','ม':',','ใ':'.','ฝ':'/' /* ... */ };
```

**ลำดับการทำงาน**

```
1. ค้นด้วยคำที่พิมพ์มาก่อน
2. ไม่เจอเลย + มีตัวอักษรไทยปน → แปลงแล้วค้นใหม่
3. คืนคำที่ใช้ค้นจริง (used) + ธง swapped ออกไปด้วย
```

🚨 **ต้องทำ 2 อย่างคู่กันเสมอ ขาดข้อใดข้อหนึ่งไม่ได้**

| ต้องทำ | ถ้าไม่ทำ |
|---|---|
| **ไฮไลต์ด้วย `used`** ไม่ใช่คำที่ผู้ใช้พิมพ์ | เจอของแต่ไม่ไฮไลต์อะไรเลย เพราะ `ทำะดน` ไม่มีในคำว่า Metformin |
| **โชว์ป้าย "ค้นว่า metfo"** (ดูข้อ 10) | ผู้ใช้งงว่าพิมพ์ไทยแล้วทำไมเจอของภาษาอังกฤษ |

⚠️ **คนละเรื่องกับ "ตัวแปลงอักษรต่างด้าว" ของเว็บ HCV** อันนั้นแก้ตัวอักษรเพี้ยนจากการก๊อป
ข้อความมา (encoding พัง) · อันนี้แป้นพิมพ์คนละภาษา ตัวอักษรถูกต้องทุกตัว แค่ผิดภาษา

## 4. ค้นด้วยชื่อการค้าและตัวย่อ

เภสัชกรจำ `Augmentin` ได้ก่อน `Amoxicillin + Clavulanic acid`
และเรียก `CPM` `HCTZ` `INH` มากกว่าชื่อเต็ม

🚨🔴 **ตัวย่อต้องมาจากฐานข้อมูล ห้ามคิดขึ้นเอง** (คอลัมน์ `drugs.abbrev`)
12 ส.ค. 2569 แคลร์เคยใส่ตัวย่อเข้าฐานจริง 127 ตัวโดยคิดเอง (`METRO` `AMLO` `SIM`)
**พี่กันด่าหนักมาก** — ตัวย่อยาที่คิดเองคือข้อมูลทางการแพทย์ปลอม

## 5. ของที่ใช้บ่อยขึ้นก่อน

```sql
create or replace function mr_hot_drug_ids(p_from date, p_to date, p_limit integer)
returns jsonb language sql stable as $$
  select coalesce(jsonb_agg(drug_id order by times desc), '[]'::jsonb)
  from (select drug_id, count(*) as times from mr_return
        where deleted_at is null and drug_id is not null
        group by drug_id order by count(*) desc limit coalesce(p_limit,20)) t;
$$;
```

ส่งมาตอนเปิดเว็บ (`/api/bootstrap`) เพราะช่องค้นหาใช้ทันที
🚨 **เป็นตัวตัดสินอันดับที่ 3 เท่านั้น ยังแพ้ "เจอที่ต้นชื่อ" เสมอ**

## 6. จัดกลุ่มตามรูปแบบยา

**พี่กันเคาะ 13 ส.ค. 2569** หลังเห็นว่า Morphine 5 รายการเรียงสลับกันจนหาไม่เจอ

```
Morphine sulfate 10 mg/5mL  syrup      ← ยาน้ำ
Morphine sulfate 10 mg      tab (ER)   ← เม็ด
Morphine sulfate 10 mg/mL   injection  ← ยาฉีด
Morphine sulfate 20 mg      cap (ER)   ← แคปซูล
Morphine sulfate 30 mg      tab (ER)   ← เม็ด
```

เม็ดสองตัวอยู่คนละปลาย ทั้งที่เป็นของที่หยิบมาด้วยกันบ่อยที่สุด

**เหตุผลที่เรียงด้วยรูปแบบยา ไม่ใช่ความแรง** — เวลารับยาคืน สิ่งแรกที่เภสัชกรเห็นคือ
**รูปของยาในมือ** (เม็ด ขวด หลอด) ไม่ใช่ตัวเลขความแรง

```js
const FORM_ORDER = [
  'tab', 'cap',                                                            // ยากินชนิดแข็ง
  'syrup', 'suspension', 'solution', 'elixir', 'oil', 'sachet', 'powder',  // ยากินชนิดน้ำ/ผง
  'injection', 'vial', 'amp', 'prefilled syringe', 'prefilled pen',        // ยาฉีด
  'nebule', 'MDI', 'inhaler', 'turbuhaler', 'spray',                       // ยาพ่น
  'eye drops', 'drops', /* ... ยาใช้ภายนอกท้ายสุด */
];
```

---

# 🔍 ส่วนที่ 2 · หน้าตาช่องค้นหา

## 7. 🚨 ทุกช่องต้องเรียกตัวกลางตัวเดียว ห้ามวาดเอง

**พี่กันสั่ง 3 ก.ย. 2569**

> *"ทำกรอบค้นหา ให้เหมือนกันที คือมีไอค่อนเเว่นขยาย ทำเป็นกฎ
>  เหมือนกรอบค้นนี้ใน lot ทั้งการป้องกันไม้โทหาย การจัดตรงกลาง"*

**ปัญหาเดิม** — เว็บยาคืนมีช่องค้นหา **8 ช่อง** วาดกันคนละแบบ บางช่องมีแว่นขยาย บางช่องไม่มี ·
บางช่องตัดวรรณยุกต์ · ปุ่มล้างคำค้นขนาดไม่เท่ากันสักช่อง

**ตาผู้ใช้จำรูปทรง ไม่ได้จำตำแหน่ง** ช่องที่หน้าตาไม่เหมือนกันทำให้ต้องมองซ้ำทุกครั้ง
ว่าช่องนี้คือช่องค้นหาหรือช่องกรอกข้อมูล

| เว็บ | ไฟล์ต้นฉบับ | ตัวที่เรียก |
|---|---|---|
| ยาคืน | `components/pages/thaibox.jsx` | `renderSearchBox({...})` |
| ME-DRP | `components/SearchBox.tsx` (TypeScript) | `<SearchBox ... />` |

```js
{renderSearchBox({
  value: V.q, onChange: V.onQ, onClear: V.clearQ,
  placeholder: 'ค้นเลข Lot ชื่อผู้บันทึก หรือชื่อ รพ.สต.',
  font: '400 13px/1.75 var(--font-sarabun), Sarabun, sans-serif',
  h: 44,                    // ความสูงกรอบที่ตาเห็น (มือถือ 44 · คอม 42)
  bg: '#f6f7f4',            // ไม่ใส่ = ขาว
  swapLabel: V.swapped ? V.swapLabel : '',
  ariaLabel: 'ค้นหาในรายการ Lot',
})}
```

🚨🔴 **ขนาดทุกอย่างคำนวณจาก `h` ตัวเดียว** — ไอคอน ปุ่มล้าง ระยะขอบ ระยะบรรทัด
เปลี่ยนความสูงกรอบแล้วทุกอย่างขยับตามเอง ไม่ต้องไล่แก้ทีละค่าแล้วลืมบางตัว

**กรอบนอก** — `border-radius:10px` · `border:1px solid rgba(30,36,32,.14)` · `flex:1;min-width:0`

## 8. ไอคอนแว่นขยาย

```jsx
const ไอคอน = Math.round(h * 0.36);        // h=44 → 16px · h=42 → 15px

<svg width={ไอคอน} height={ไอคอน} viewBox="0 0 24 24" fill="none" stroke="#6f7873"
  strokeWidth="2.2" strokeLinecap="round" aria-hidden="true"
  style={{ position:'absolute', left:'12px', top:'50%',
           transform:'translateY(-50%)', pointerEvents:'none' }}>
  <circle cx="11" cy="11" r="7" /><path d="M20 20l-3.5-3.5" />
</svg>

padLeft: (ไอคอน + 20) + 'px'      // ที่เว้นซ้ายของช่องกรอก
```

| ค่า | เท่าไหร่ | ทำไม |
|---|---|---|
| ขนาด | **0.36 เท่าของ `h`** | ผูกกับตัวเดียว |
| สี | **`#6f7873`** | ผ่านเกณฑ์อ่านง่าย 5.0:1 · **ห้ามใช้ `#9aa19c` ตกเกณฑ์ (2.4:1)** |
| ความหนาเส้น | `2.2` ปลายมน | บางกว่านี้จางหายบนจอห้องยาที่แสงจ้า |
| ที่เว้นซ้ายของช่องกรอก | **ไอคอน + 20** | น้อยกว่านี้ตัวอักษรทับไอคอน |

🚨 **วาด SVG ในไฟล์เอง ห้ามลงไลบรารีไอคอนเพื่อรูปเดียว** วงกลมกับด้าม สองเส้นจบ
🚨 **ต้องมี `pointerEvents:'none'`** ไม่งั้นกดโดนไอคอนแล้วช่องไม่โฟกัส ผู้ใช้กดแล้วพิมพ์ไม่ได้
🚨 **ต้องมี `aria-hidden="true"`** ไอคอนเป็นของประดับ โปรแกรมอ่านจอไม่ต้องอ่าน

## 9. ปุ่ม ✕ ล้างคำค้น

```jsx
const ปุ่ม = h - 16;                        // h=44 → 28px

{มีปุ่มล้าง && (
  <div {...kb(o.onClear)} aria-label="ล้างคำค้น" title="ล้างคำค้น" className="hv-bg-f6"
    style={{ position:'absolute', right:'7px', top:'50%', transform:'translateY(-50%)',
             width: ปุ่ม+'px', height: ปุ่ม+'px', borderRadius:'7px',
             display:'flex', alignItems:'center', justifyContent:'center',
             cursor:'pointer', color:'#6b746e',
             font:'400 ' + Math.round(h*0.31) + 'px/1 Sarabun, sans-serif' }}>✕</div>
)}

padRight: มีปุ่มล้าง ? (h - 8) + 'px' : '13px'
```

🚨 **โผล่เฉพาะตอนมีข้อความ** (`มีคำ = !!String(value).trim()`) ปุ่มที่โผล่ตลอดเวลาทั้งที่กดแล้ว
ไม่เกิดอะไรคือปุ่มหลอก
🚨 **ต้องกดด้วยคีย์บอร์ดได้** ใช้ `kb()` ไม่ใช่ `onClick` เปล่า ๆ
🚨 **ต้องมี `aria-label="ล้างคำค้น"`** ปุ่มที่มีแต่ไอคอนต้องมีชื่อเสมอ
🚨 **ที่เว้นขวาของช่องกรอกต้องขยับตามปุ่ม** ไม่งั้นตัวอักษรลอดไปใต้ปุ่ม

⚠️ **ปุ่มล้างต้องล้างทั้งคำค้นและตัวกรองที่เกี่ยวข้อง** ถ้าหน้านั้นมีตัวกรองอยู่ด้วย
เคยพลาดมาแล้ว — ล้างแค่ตัวกรอง คำค้นยังค้าง ผลลัพธ์เลยยังว่างโดยไม่รู้สาเหตุ

## 10. ป้าย "ค้นว่า ..."

โผล่ตอนระบบแปลงแป้นพิมพ์ให้ (ข้อ 3) — ไม่มีป้ายนี้ ผู้ใช้จะงงว่าพิมพ์ไทยแล้วทำไมเจอของอังกฤษ

```jsx
{!!o.swapLabel && (
  <span style={{ position:'absolute', top:'50%', transform:'translateY(-50%)',
                 right: (มีปุ่มล้าง ? h-6 : 10) + 'px',
                 font:'600 11px/1.75 Sarabun,sans-serif',
                 color:'#2f7d5d', background:'#e7f2ec', borderRadius:'6px',
                 padding:'3px 8px', whiteSpace:'nowrap', pointerEvents:'none' }}>
    ค้นว่า {o.swapLabel}
  </span>
)}

padRight: o.swapLabel ? '150px' : ...     // ที่เว้นขวาต้องพอดีกับสิ่งที่โผล่จริง
```

🚨 **ที่เว้นขวาต้องเปลี่ยนตามว่ามีป้ายไหม** ไม่งั้นตัวหนังสือลอดไปใต้ป้าย

## 11. 🔴 วรรณยุกต์ไทยห้ามโดนตัด

**พี่กันเจอเอง 1 ก.ย. 2569 แล้วสั่งซ้ำสี่รอบกว่าจะแก้ถูก**

**อาการ** — คำที่มีวรรณยุกต์ซ้อนสระบน (**นี้ · นั้น · ชื่อ · เครื่อง · ไต้**) ยอดวรรณยุกต์โดนหั่น
บางทีหายครึ่งตัว บางทีหายทั้งตัว **ตัวอักษรอื่นปกติทุกอย่าง จึงมองข้ามง่ายมาก**

🔑 **คำเทียบที่พี่กันให้มา: `ก้`** ไม้โทบนพยัญชนะเปล่าอยู่ต่ำ ไม่โดนตัด
เอาไว้ดูว่าไม้โทเต็มตัวหน้าตาเป็นยังไง แล้วเทียบกับ `นี้ นั้น`

### ต้นเหตุ

**ช่องกรอกทุกชนิดตัดตัวอักษรที่ล้นออกนอกตัวเองเสมอ ปิดไม่ได้**
สั่ง `overflow:visible` ก็ถูกเบราว์เซอร์เปลี่ยนเป็นตัดอัตโนมัติ
(Chromium bug 339052 · W3C bug 17473) เพราะเว็บเก่าจำนวนมากพึ่งพาพฤติกรรมนี้

ตัวอักษรไทยที่มีวรรณยุกต์ซ้อนสระบน **ลอยสูงกว่าที่ฟอนต์ประกาศไว้ราว 2-3 จุด**

### ❌ วิธีที่ลองแล้วไม่ได้ผล (อย่าเสียเวลาซ้ำ)

| วิธี | ทำไมไม่ได้ผล |
|---|---|
| เพิ่มระยะบรรทัด | ช่องกรอกยังตัดที่เดิม ไม่เกี่ยวกับระยะบรรทัด |
| ถอดความสูงตายตัวออก | ช่องสูงตามเนื้อหาแล้วก็ยังตัด |
| ดันข้อความลงด้วยระยะขอบบน | กล่องข้อความจัดกึ่งกลางในช่องเสมอ ดันไม่ลง แถมทำให้ไม่กลาง |
| เปลี่ยนฟอนต์ | ทุกฟอนต์ไทยเจอเหมือนกันหมด |
| ทำช่องกรอกสูงเกินกรอบ แล้วหักด้วยขอบติดลบ | ยังตัดที่ความสูงของฟอนต์อยู่ดี |
| เปลี่ยนเป็น `<textarea>` | โดนบังคับเหมือนกัน วัดค่าจริงได้ `auto` ทั้งที่สั่ง `visible` |
| สั่ง `overflow-x` อย่างเดียว | กฎ CSS: แกนหนึ่งตัด อีกแกนที่สั่งปล่อยล้นจะถูกเปลี่ยนเป็นตัดตาม |

### ✅ วิธีที่ได้ผลจริง — โครงสามชั้น

```jsx
<div style={กรอบที่ตาเห็น · position:relative · height เท่าเดิม}>

  {/* ② ชั้นวาด — กล่องธรรมดา ปล่อยล้นได้ · สูงเกินกรอบ 7 จุดบนล่าง
      แล้วหักคืนด้วยตำแหน่งติดลบ ผังหน้าเว็บจึงนับความสูงเท่าเดิม */}
  <div ref={drawRef} aria-hidden="true"
    style={'position:absolute;left:0;right:0;top:-7px;bottom:-7px;overflow:hidden;' +
           'display:flex;align-items:center;pointer-events:none;padding:<เท่าช่องกรอก>'}>
    <span style={{ font:'<ชุดเดียวกับช่องกรอก>', lineHeight: innerH + 'px',
                   color: value ? 'สีปกติ' : 'สีข้อความใบ้' }}>
      {value || placeholder}
    </span>
  </div>

  {/* ③ ช่องกรอกจริง — ตัวอักษรโปร่งใส เหลือแต่ขีดกะพริบ */}
  <input value={value} onChange={...} onScroll={syncScroll} onBlur={resetScroll}
    style={{ position:'relative', width:'100%', height:'100%', border:'none',
             background:'transparent', caretColor:'สีปกติ', color:'transparent',
             font:'<ชุดเดียวกับชั้นวาด>', padding:'<เท่าชั้นวาด>' }} />
</div>
```

🚨 **ฟอนต์ ขนาด น้ำหนัก ระยะขอบ ต้องตรงกันเป๊ะทั้งสองชั้น** ไม่งั้นขีดกะพริบไม่ตรงตัวอักษร
🚨 **ต้องเลื่อนตามกัน** — `onScroll` ของช่องกรอกเขียน `scrollLeft` ให้ชั้นวาด
ไม่งั้นพิมพ์ยาวเกินช่องแล้วตัวอักษรค้างที่เดิม ขีดกะพริบวิ่งหนีไปคนละที่
🚨 **เลิกโฟกัสแล้วต้องเลื่อนกลับ** (`onBlur`)
🚨 **ชั้นวาดต้อง `pointer-events:none`** ไม่งั้นกดไม่โดนช่องกรอก
🚨 **ใส่ `aria-hidden="true"` ให้ชั้นวาด** ไม่งั้นโปรแกรมอ่านจออ่านซ้ำสองรอบ

### 📏 ข้อความธรรมดา (ไม่ใช่ช่องกรอก) แก้ง่ายกว่ามาก

กล่องธรรมดาปล่อยล้นได้อยู่แล้ว แค่**ตั้งระยะบรรทัดไม่ต่ำกว่า 1.5 เท่า**ของขนาดตัวอักษร
🚨 **จุดที่มี `overflow:hidden` + `white-space:nowrap` เสี่ยงที่สุด** เพราะกล่องสูงเท่าบรรทัดพอดี

### 🔴🔴🔴 ไม่ใช่แค่ช่องค้นหา — **ช่องกรอกข้อความไทยทุกช่องในเว็บ**

**พี่กันจับได้ 5 ก.ย. 2569** จากช่อง "ระบุลักษณะเพิ่มเติม…" ในหน้ากรอก

> *"คำว่า ระบุ สระอุ มันโดนตัด · จุดนี้ควรเป็นกฎเดียวกันกับช่องค้นหาไหม"*
> *"ไม่เอา 6 จุด **ปรับช่องให้เป็นระบบเดียวกันทั้งหมด**"*

🔑 **สระล่างโดนตัดด้วยกลไกเดียวกับสระบนเป๊ะ** — `<input>` ตัดทุกอย่างที่ล้นออกนอกตัวเอง
ไม่ได้เลือกว่าล้นบนหรือล้นล่าง · ที่ผ่านมาเห็นแต่ไม้โทหายเพราะคำที่ทดสอบมีแต่สระบน

**เดิมกฎข้อนี้เขียนไว้ในบริบทช่องค้นหาอย่างเดียว** จึงไปแก้แค่ช่องค้นหา
ทั้งที่ช่อง "ระบุ…" · ช่องชื่อยา · ช่องพิมพ์ยืนยันลบถาวร โดนเหมือนกันหมด

### ✅ ตัวกลางมี 2 ตัว เลือกตามหน้าที่ของช่อง

| ช่องแบบไหน | ใช้ตัวไหน | ได้อะไร |
|---|---|---|
| **ช่องค้นหา** | `<SearchBox>` | แว่นขยาย + ปุ่มล้าง + ป้าย "ค้นว่า" + กันสระตัด |
| **ช่องกรอกข้อความทั่วไป** | `<ThaiTextInput>` | กันสระตัด + ขอบโฟกัสมาตรฐาน (ไม่มีแว่นขยาย) |

🚨 **ห้ามเอา `SearchBox` ไปใช้กับช่องกรอกข้อมูล** — ไอคอนแว่นขยายทำให้คนเข้าใจผิดว่า
พิมพ์แล้วระบบจะไปค้นอะไรให้ ทั้งที่เป็นช่องกรอกข้อมูลธรรมดา

### ช่องไหนต้องแปลง ช่องไหนไม่ต้อง

| ชนิดช่อง | ต้องใช้ตัวกลางไหม | เพราะ |
|---|:--:|---|
| ข้อความไทย | ✅ | สระบน/ล่างโดนตัด |
| ชื่อยา (อังกฤษเป็นหลัก) | ✅ | อยู่กล่องเดียวกับช่องไทย ต้องสูงเท่ากัน |
| `type="date"` | ❌ | ไม่มีตัวอักษรไทย |
| `type="tel"` · `inputMode="numeric"` | ❌ | เลขล้วน |
| **`<textarea>`** | ❌ **ถ้าวัดแล้วผ่านเกณฑ์** | ดูหัวข้อถัดไป |

### 🔑 `<textarea>` ไม่ต้องแก้ ถ้าระยะบรรทัดกับขอบในพอ — แต่ต้องวัดก่อน

**วัดของจริงใน ME-DRP 5 ก.ย. 2569** — ทุกช่องได้

```
ขนาดตัวอักษร 15px · ระยะบรรทัด 23.25px (= 1.55 เท่า) · ขอบในบน-ล่าง 12px
```

`<textarea>` ต่างจาก `<input>` ตรงที่ตัดที่**ขอบกล่อง** ไม่ใช่ตัดทุกอย่างที่ล้นบรรทัด
ระยะบรรทัด 1.55 เท่าจึงเผื่อที่ให้สระบน-ล่างพออยู่แล้ว

🚨 **ต้องวัดก่อนสรุปว่าไม่ต้องแก้** — เกณฑ์คือ **ระยะบรรทัด ≥ 1.5 เท่า** และ **ขอบในบน-ล่าง ≥ 10px**
ต่ำกว่านี้เมื่อไหร่ต้องทำตัวกลางของ textarea เพิ่ม

```js
// วางในคอนโซล — บอกว่า textarea ในหน้านี้ผ่านเกณฑ์ไหม
[...document.querySelectorAll('textarea')].map(t => {
  const cs = getComputedStyle(t);
  const เท่า = parseFloat(cs.lineHeight) / parseFloat(cs.fontSize);
  return { เท่า: เท่า.toFixed(2), ขอบใน: cs.paddingTop, ผ่าน: เท่า >= 1.5 && parseFloat(cs.paddingTop) >= 10 };
});
```

### 🚨 นับจุดก่อนบอกว่าเสร็จ

```bash
# ① ช่องกรอกที่ยังใช้ input ธรรมดา — ต้องเหลือแต่ date/tel/numeric
grep -rn "<HInput" -A 6 --include=*.tsx components/ | grep -E 'type="|inputMode="' \
  | grep -oE 'type="[^"]*"|inputMode="[^"]*"' | sort | uniq -c

# ② จุดที่เรียกตัวกลางแล้ว
grep -rc "<ThaiTextInput\|<SearchBox" --include=*.tsx components/ | grep -v ":0"
```

**ME-DRP หลังทำครบได้** — ตัวกลาง 13 จุด · ที่เหลือเป็น `date` 8 · `tel` 2 · `numeric` 2
ไม่มีช่องข้อความไทยที่ยังใช้ `input` ธรรมดาเลยสักช่อง

### 🔴🔴 กับดักที่ 2 — `overflow:hidden` + `nowrap` ตัดสระโดยไม่ต้องเป็นช่องกรอกเลย

**พี่กันจับได้จากป้าย "— เลือกผู้รายงาน —" ในช่องเลือก** (5 ก.ย. 2569 · *"สระอู"*)
ช่องนั้นไม่ใช่ `<input>` ด้วยซ้ำ เป็นแค่ `<span>` ในปุ่มดรอปดาวที่ทำเอง

**ต้นเหตุ** — กล่องที่มี 3 อย่างนี้พร้อมกันจะสูงเท่าบรรทัดพอดี สระที่ล้นจึงโดนตัด

```
overflow:hidden  +  white-space:nowrap  +  ไม่ได้ตั้ง line-height
```

`line-height: normal` ของ Sarabun ≈ **1.27 เท่า** ซึ่งไม่พอสำหรับสระบน/ล่าง
วัดของจริง: span สูง **19px** ที่ตัวอักษร 15px → พอตั้ง `line-height:1.75` เป็น **26px** สระครบ

✅ **ทางแก้ = ตั้ง `line-height:1.75` ที่กล่องนั้น** ไม่ต้องรื้อโครงสามชั้นเหมือนช่องกรอก
(กล่องธรรมดาปล่อยล้นได้ แค่ต้องมีที่ให้ล้น)

🚨 **`overflow:hidden` ยังต้องอยู่** — มันทำหน้าที่ตัดข้อความยาวด้วยจุดไข่ปลา ไม่ใช่ตัวผิด
ตัวผิดคือ **กล่องเตี้ยเกิน** ไม่ใช่การตัดเอง

**คำสั่งกวาดหาจุดเสี่ยงทั้งเว็บ**

```bash
grep -rn "overflow:hidden" --include=*.tsx --include=*.jsx components/ \
  | grep -E "nowrap|ellipsis" | grep -v "line-height"
```

**ME-DRP เจอ 6 จุดในวันเดียว** — ป้ายดรอปดาวผู้รายงาน · รายการตัวเลือกในดรอปดาว ·
ชื่อยาที่ตัดด้วยจุดไข่ปลา · ชื่อคนกรอกในการ์ดรายงาน
**ทุกจุดเป็นข้อความไทยที่มีสระบนหรือสระล่างทั้งนั้น**

🔑 **บทเรียน — กฎนี้ไม่ได้ผูกกับ "ช่องกรอก" แต่ผูกกับ "กล่องที่เตี้ยเท่าบรรทัด"**
เดิมเขียนไว้ในบริบทช่องค้นหาอย่างเดียว จึงมองข้ามป้ายกับดรอปดาวไปหมด

## 12. 🎯 ตัวอักษรในชั้นวาดต้องนั่งตรงกับช่องกรอก

**พี่กันส่งภาพมา 3 รอบ** — *"คำที่ค้น อยู่กลางกรอบยัง"* · *"เราว่ามันอยู่บนๆไป"*

### ต้นเหตุ — สองชั้นจัดกึ่งกลางคนละกลไก

**`<input>` จัดตัวอักษรกึ่งกลางด้วยกลไกของตัวเอง โดยไม่สนระยะบรรทัดที่สั่ง**
ส่วนกล่องธรรมดาเคารพระยะบรรทัด → เส้นฐานของสองชั้นอยู่คนละที่

⚠️ **วัดกล่องแล้วจะบอกว่าถูก** — กล่องตัวอักษรอยู่กลางกรอบเป๊ะ เยื้อง 0 จุด
สิ่งที่เยื้องคือ **ตัวอักษรข้างในกล่อง** ซึ่งการวัดกรอบจับไม่ได้

```js
// ❌ ผิด — ระยะบรรทัด 1.75 เท่า ทำให้เส้นฐานคนละที่กับช่องกรอก
<span style={{ font: '400 13px/1.75 Sarabun' }}>

// ✅ ถูก — ระยะบรรทัดเท่ากับ "ความสูงในกรอบ" (ความสูงกรอบ ลบขอบบนล่าง)
<span style={{ font: '400 13px/1.75 Sarabun', lineHeight: (h - 2) + 'px' }}>
```

ยังเหลือที่ว่างบนล่างข้างละ 7 จุดให้วรรณยุกต์เท่าเดิม เพราะชั้นวาดยังยื่นออกนอกกรอบด้วย
`top:-7px;bottom:-7px`

🔑 **ตรวจด้วยไอคอนที่อยู่ในช่องเดียวกัน** — แว่นขยายจัดกึ่งกลางด้วย `top:50%;translateY(-50%)`
ซึ่งกลางจริงเสมอ **ตัวอักษรต้องอยู่ระดับเดียวกับไอคอน** ถ้าเยื้องคือผิด
เร็วและเชื่อได้กว่าการวัดตัวเลข

---

# 🎨 ส่วนที่ 3 · ผลค้นหาบนจอ

## 13. ไฮไลต์คำที่ค้นเจอ

```js
export function markMatch(text, query) { /* ตัดเป็น 3 ท่อน: ก่อน · ที่ตรง · หลัง */ }
```

| ส่วน | สี |
|---|---|
| คำที่ตรงกับที่ค้น | ตัวอักษร `#2f7d5d` บนพื้น `#dcefe4` น้ำหนัก 700 |

🚨🔴 **ต้องไฮไลต์ด้วย `used` ไม่ใช่คำที่ผู้ใช้พิมพ์** (ดูข้อ 3)
พิมพ์ไทยลืมสลับแป้นแล้วไฮไลต์ด้วยคำเดิม = เจอของแต่ไม่ไฮไลต์อะไรเลย

## 14. 🔴 สีความแรงตอนชื่อซ้ำ → **กฎเต็มอยู่ในสกิล `pharmacy-web-logic` ข้อ 26**

`Morphine 10 · 20 · 30 mg` หยิบสลับกันแล้วอันตรายถึงชีวิต **ตาต้องจับได้ตั้งแต่กวาดผ่าน**
พี่กันสั่ง 13 ส.ค. 2569: *"ยาที่ตัวเดียวกันและคนละความแรงก็ต้องแยกสี"*

ที่ต้องรู้เมื่อทำผลค้นหา — **ทำงานเฉพาะตอนผลค้นหามีของชื่อเดียวกันตั้งแต่ 2 รายการ**
เจอตัวเดียวคงเทาเหมือนเดิม ไม่งั้นทั้งจอเป็นสีรุ้ง สีเลยหมดความหมาย
และ **ต้องอ่านออกทั้งบนพื้นขาวและพื้นของแถวที่ถูกลูกศรเลือก**

⚠️ **กฎเต็ม (ชุดสี 5 สี · สีที่จองไว้ห้ามใช้ซ้ำ · วิธีจับกลุ่ม) อยู่ในสกิลห้องยา ไม่ใช่ที่นี่**
เพราะสีความแรงไม่ได้ใช้แค่ในผลค้นหา — ตารางรายการกับหน้าต่างแก้ไขก็ใช้
เป็นเรื่อง **การแสดงชื่อยา** ไม่ใช่เรื่องค้นหา

## 15. ลูกศรเลื่อน กรอบต้องเลื่อนตาม

กรอบผลค้นหาสูงราว 290px เห็นราว 5 แถว
กดลูกศรลงไปแถวที่ 6 ตัวที่ถูกเลือกจะซ่อนใต้ขอบกรอบ **ผู้ใช้กดต่อแบบตาบอด**

```jsx
componentDidUpdate(prevProps, prevState) {
  if (prevState.hi !== this.state.hi && this.hiRef.current) {
    this.hiRef.current.scrollIntoView({ block: 'nearest' });
  }
}
```

`block:'nearest'` = เลื่อนน้อยที่สุดเท่าที่จำเป็น แถวที่เห็นอยู่แล้วไม่ขยับ

🚨 **ทำใน `componentDidUpdate` ไม่ใช่ ref callback** เพราะ ref ถูกเรียกทุกครั้งที่วาดจอ
จะดึงกรอบกลับตอนผู้ใช้เลื่อนดูเองด้วยเมาส์

## 16. ลูกศรวนรอบ

```js
export function moveHi(current, delta, total) {
  if (!total) return -1;
  if (current < 0) return delta > 0 ? 0 : total - 1;  // ยังไม่ชี้ · กดขึ้น = ไปตัวสุดท้าย
  let next = current + delta;
  if (next < 0) next = total - 1;
  if (next >= total) next = 0;
  return next;
}
```

## 17. กรอบผลค้นกันหน้าเพจไหลตาม

```css
overscroll-behavior: contain;
```

เลื่อนดูผลค้นจนสุดกรอบแล้วหน้าเว็บข้างหลังจะไหลตามต่อ **ทำให้ตำแหน่งที่กำลังดูหาย**
ใส่บรรทัดเดียวจบ ไม่เปลี่ยนหน้าตาสักพิกเซล

---

# ✅ วิธีตรวจว่าทำครบจริง

## 🚨🔴 สร้างตัวกลางแล้วต้องนับจุดที่เรียกใช้ ก่อนบอกว่าเสร็จ

```bash
# ① มีช่องค้นหากี่ช่อง
grep -rn 'placeholder=.*ค้น' --include=*.tsx --include=*.jsx components/ app/ | wc -l

# ② มีกี่จุดที่เรียกตัวกลาง (ต้องเท่ากับข้อ ①)
grep -rn "SearchBox\|renderSearchBox" --include=*.tsx --include=*.jsx components/ app/ \
  | grep -v "components/SearchBox\|pages/thaibox" | wc -l
```

**ME-DRP เคยได้ ① = 4 · ② = 0** — สร้างไฟล์ 241 บรรทัดไว้ครบ แต่ไม่มีใครเรียก
พี่กันจับได้เอง 5 ก.ย. 2569 · **สร้างไว้แล้วไม่ต่อสาย เท่ากับไม่ได้ทำ**

## คำสั่งตรวจอื่น

```bash
# มีไฟล์สมองการค้นหาไหม
ls lib/drugSearch.js

# กรอบเลื่อนตามลูกศรไหม
grep -rn "scrollIntoView" components/

# หาจุดที่ระยะบรรทัดต่ำกว่า 1.5 เท่า (เสี่ยงตัดวรรณยุกต์)
grep -rEoh "[0-9]+(\.[0-9]+)?px/1\.[0-4][0-9]?" --include=*.tsx --include=*.jsx --include=*.js \
  components/ app/ lib/ | sort | uniq -c | sort -rn
```

ทดสอบสมองการค้นหาได้โดยไม่ต้องเปิดเบราว์เซอร์

```js
// node test.mjs
import { searchDrugs, thaiToEnglish } from './lib/drugSearch.js';
console.log(searchDrugs(drugs, 'amox clav'));      // ต้องเจอยาผสม
console.log(thaiToEnglish('ทำะดนพทรื'));           // ต้องได้ metformin
```

เว็บยาคืนมี `scripts/thai-clip-check.mjs` สแกนทั้งหน้าให้

## 🚨🔴 ข้อห้ามเรื่องการพิสูจน์

**วรรณยุกต์ไทยพิสูจน์ด้วยภาพหน้าจอไม่ได้** วรรณยุกต์เล็กระดับ 1-2 จุด
ตัวตรวจเคยฟ้องผิดว่าชิปหนึ่งมีปัญหาทั้งที่ปกติ และเคยบอกว่าผ่านทั้งที่พี่กันเห็นว่าตัด

**ต้องพิมพ์ `นี้ นั้น ก้ ชื่อ เครื่อง` ลงช่องจริง แล้วให้พี่กันดูด้วยตา**
🔴 **พี่กันบอกว่ายังโดนตัด = ยังโดนตัด**

---

# 🔴 ความผิดที่เคยทำ ห้ามซ้ำ

| เมื่อไหร่ | ทำอะไรผิด |
|---|---|
| 5 ก.ย. 2569 | **สร้าง `SearchBox.tsx` ใน ME-DRP เสร็จ แล้วไม่ได้ต่อสายให้ช่องไหนเรียก** พี่กันจับได้ |
| 5 ก.ย. 2569 | เขียนกฎแว่นขยายไว้แค่ว่า "ต้องมี" **ไม่มีขนาด สี ตำแหน่งสักตัว** คนไปทำเว็บอื่นเลยต้องเดาเอง |
| 5 ก.ย. 2569 | **แตะหน้าคลังยาทั้งวันโดยไม่เปิดสกิลอ่านเลย** ทั้งที่สกิลเขียนกำกับว่าให้อ่านก่อนลงมือ |
| 5 ก.ย. 2569 | สั่งให้ "ไปอ่าน" แล้ว **grep หาคำที่นึกออกแทนอ่านทั้งหัวข้อ** เจอกฎข้อเดียวแล้วรายงานว่าครบ |
| 5 ก.ย. 2569 | **สร้างช่องค้นหาขึ้นใหม่เอง ทั้งที่มีของกลางอยู่แล้ว** ไม่ได้หาก่อนว่ามีมาตรฐานกลางไหม |
| 1 ก.ย. 2569 | เชื่อตัวตรวจกับภาพหน้าจอว่าวรรณยุกต์ผ่านแล้ว ทั้งที่พี่กันเห็นว่ายังตัด |
| 12 ส.ค. 2569 | **คิดตัวย่อยาขึ้นเองแล้วเขียนลงฐานจริง 127 ตัว** (`METRO` `AMLO` `SIM`) |

🔴🔴🔴 **แตะช่องค้นหาหรือระบบค้นหาเมื่อไหร่ เปิดสกิลนี้อ่านให้จบก่อนเสมอ**
ไม่ใช่รอให้พี่กันถามว่า "ทำตามกฎรึยัง" — คำถามนั้นแปลว่าพลาดไปแล้ว

---

## 18. 🔴🔴🔴 คำไทยสองคำที่ตาเห็นเหมือนกันเป๊ะ แต่ระบบมองเป็นคนละคำ

**ความรู้ภาษาไทยที่ไม่มีที่ไหนในสกิลเลย** — เจอที่เว็บ listless ซึ่งใช้ Google Sheet

### เรื่องคืออะไร

คำว่า **`กี่`** เขียนได้สองแบบ ขึ้นกับว่าวางเครื่องหมายไหนก่อน

```
แบบ A   ก + สระอี + ไม้เอก
แบบ B   ก + ไม้เอก + สระอี
```

**ตาคนมองเห็นเหมือนกันทุกประการ** วางซ้อนกันแล้วแยกไม่ออก
แต่คอมพิวเตอร์เก็บเป็นข้อมูลคนละชุด **จึงเป็นคนละคำสำหรับระบบ**

### ผลที่ตามมา

| เกิดอะไร | อาการ |
|---|---|
| ค้นหา | พิมพ์แบบ A หาแบบ B ไม่เจอ **ทั้งที่คำอยู่ตรงนั้น** |
| นับจำนวน | คนเดียวกันถูกนับเป็นสองคน |
| จับกลุ่ม | ข้อมูลของคนเดียวกันแตกเป็นสองก้อน |
| เทียบว่าซ้ำไหม | ระบบบอกว่าไม่ซ้ำ ทั้งที่ซ้ำ |

### 🚨🔴 วิธีแก้มาตรฐานสากลใช้ไม่ได้กับภาษาไทย

> *"`normalize('NFC')` **does not fix it** — verified · Unicode has no canonical order for Thai marks"*

คำสั่ง `normalize('NFC')` เป็นวิธีที่โปรแกรมเมอร์ทั่วโลกใช้จัดระเบียบตัวอักษรให้เป็นแบบมาตรฐาน
**แต่มาตรฐาน Unicode ไม่ได้กำหนดลำดับสำหรับเครื่องหมายภาษาไทย** — พิสูจน์แล้วว่าไม่แก้ให้

### วิธีที่ต้องทำ — เรียงเครื่องหมายเอง

ทำ **กุญแจสำหรับเทียบ** ที่จัดเรียงสระกับวรรณยุกต์ของแต่ละพยัญชนะให้เป็นลำดับเดียวกันเสมอ
พร้อมล้างของที่มองไม่เห็นออกด้วย (อักขระความกว้างศูนย์ · เครื่องหมายลำดับไบต์ · ยัติภังค์อ่อน ·
ช่องว่างชนิดแปลก · ช่องว่างซ้อนหลายตัว)

🚨🔴 **กุญแจนี้ใช้เทียบอย่างเดียว ห้ามเอาไปแสดงบนจอ และห้ามเขียนกลับลงฐานเด็ดขาด**
เพราะมันคือรูปแบบที่จัดใหม่เพื่อเทียบ ไม่ใช่ข้อความที่ผู้ใช้พิมพ์มา

```
เก็บลงฐาน   ← ข้อความจริงที่ผู้ใช้พิมพ์ (ไม่แตะ)
แสดงบนจอ    ← ข้อความจริง
เทียบ/ค้น/นับ ← กุญแจที่จัดเรียงแล้ว
```

### 🔑 ทำความสะอาดข้อความที่ "ตอนรับเข้า" ไม่ใช่ "ตอนแสดงผล"

ถ้าล้างตอนแสดงผล ข้อมูลสกปรกยังนอนอยู่ในฐาน แล้วทุกที่ที่หยิบไปใช้ต้องล้างเองซ้ำ ๆ
**ล้างที่ประตูทางเข้าครั้งเดียว ข้างในสะอาดหมด**

⚠️ เรื่องนี้คนละอย่างกับ **วรรณยุกต์โดนตัดในช่องกรอก** (ข้อ 11) —
อันนั้นเป็นเรื่องการวาดบนจอ อันนี้เป็นเรื่องข้อมูลที่เก็บ **ต้องทำทั้งสองอย่าง**
